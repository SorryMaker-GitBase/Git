1.远程分支操作命令行
    git feach --查看远程分支最新状态
    git banch -a --查看本地与远程所有分支
    git push origin brunch_name --推送本地分支到远程仓库
    git push origin :remote_branch --删除远程分支（本地分支还会保留）
    git chechout -b local_branch origin/remote_branch --拉取远程指定分支并在本地创建分支